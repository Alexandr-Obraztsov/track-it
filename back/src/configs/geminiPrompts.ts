// Промпты для взаимодействия с Gemini AI
export const GEMINI_PROMPTS = {
    AUDIO_TRANSCRIPTION: `Текущее время: {currentTime}. 

Участники группы: {members}

Роль пользователя: {userRole}

Существующие задачи: {tasks}

Пожалуйста, прослушай это аудио сообщение и извлеки из него:
1. Список ролей, которые нужно создать (если упоминаются новые роли)
2. Список новых задач или дел
3. Назначения задач на конкретных пользователей или роли
4. Операции с существующими задачами (удаление, изменение статуса, назначение)

Правила:
- Приоритет задач должен быть всегда присвоен: high, medium или low. Если не упомянут явно, присвой medium.
- Если дедлайн упомянут (например, "завтра", "через неделю", "к пятнице"), вычисли его относительно текущего времени и верни в формате YYYY-MM-DD.
- assignedToUser должен содержать точное имя пользователя из списка участников (если задача назначается на конкретного человека)
- assignedToRole должен содержать название роли (если задача назначается на роль)
- Если не указано назначение, оставь assignedToUser и assignedToRole как null
- Для операций с существующими задачами используй ID задачи из списка выше
- Учитывай роль пользователя при обработке команд: администраторы и модераторы могут выполнять все операции, обычные пользователи - ограниченные

Верни ответ в следующем JSON формате:
{
  "roles": [
    {
      "name": "название роли"
    }
  ],
  "tasks": [
    {
      "title": "краткое название задачи",
      "description": "подробное описание задачи",
      "priority": "high/medium/low",
      "deadline": "YYYY-MM-DD если дедлайн упомянут, иначе null",
      "assignedToUser": "имя пользователя из списка участников или null",
      "assignedToRole": "название роли или null"
    }
  ],
  "taskOperations": [
    {
      "operation": "delete/update/complete",
      "taskId": "ID существующей задачи",
      "updateData": {
        "title": "новое название (если операция update)",
        "description": "новое описание (если операция update)",
        "priority": "новый приоритет (если операция update)",
        "deadline": "новый дедлайн (если операция update)",
        "assignedToUser": "новый исполнитель (если операция update)",
        "assignedToRole": "новая роль (если операция update)",
        "isCompleted": true/false
      }
    }
  ]
}

Если в аудио нет задач, ролей или операций, верни соответствующие пустые массивы. Ответ должен быть только валидным JSON, без дополнительного текста.`,
} as const;