// Промпты для взаимодействия с Gemini AI
export const GEMINI_PROMPTS = {
    AUDIO_TRANSCRIPTION: `Текущее время: {currentTime}. Пожалуйста, прослушай это аудио сообщение и извлеки из него список задач или дел. Приоритет должен быть всегда присвоен: high, medium или low. Если приоритет не упомянут явно, присвой medium. Если дедлайн упомянут (например, "завтра", "через неделю", "к пятнице"), вычисли его относительно текущего времени и верни в формате YYYY-MM-DD.

Верни ответ в следующем JSON формате:
{
  "tasks": [
    {
      "title": "краткое название задачи",
      "description": "подробное описание задачи",
      "priority": "high/medium/low",
      "deadline": "YYYY-MM-DD если дедлайн упомянут, иначе null"
    }
  ]
}

Если в аудио нет задач, верни пустой массив tasks. Ответ должен быть только валидным JSON, без дополнительного текста.`,
} as const;