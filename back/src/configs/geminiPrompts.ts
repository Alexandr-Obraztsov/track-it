import { Chat } from '../entities/Chat';
import { User } from '../entities/User';
import { Task as TaskEntity } from '../entities/Task';
import { Label } from '../entities/Label';

export const GEMINI_PROMPTS = {
  // Промпт для извлечения групповых задач из текста/аудио
  extractGroupTasks: (text: string, currentTime: Date, chat: Chat, existingTasks: TaskEntity[] = [], labels: Label[] = []): string => {
    const usersInfo = chat.userChatRoles?.map(ucr => ({
      id: ucr.user.id,
      username: ucr.user.username,
      firstName: ucr.user.firstName,
      lastName: ucr.user.lastName || null,
      role: ucr.role?.title || null
    })) || [];

    const rolesInfo = chat.chatRoles?.map(cr => ({
      id: cr.role.id,
      title: cr.role.title,
      createdAt: cr.role.createdAt ? new Date(cr.role.createdAt).toISOString() : null
    })) || [];

    const existingTasksInfo = existingTasks.length > 0 
      ? existingTasks.map(task => ({
          id: task.id,
          title: task.title,
          description: task.description || null,
          assignedUserId: task.assignedUserId || null,
          assignedRoleId: task.assignedRoleId || null,
          deadline: task.deadline ? (task.deadline instanceof Date ? task.deadline.toISOString().split('T')[0] : task.deadline) : null,
          labelId: task.labelId || null,
          labelName: task.label?.name || null,
        }))
      : [];

    const labelsInfo = labels.length > 0
      ? labels.map(label => ({
          id: label.id,
          name: label.name,
          color: label.color || null,
        }))
      : [];

    return `Извлечение задач из сообщения пользователя.

КОНТЕКСТ:
- Время: ${currentTime.toISOString()}
- Чат: "${chat.title}" (ID: ${chat.id})
- Участники: ${JSON.stringify(usersInfo, null, 2)}
- Роли: ${JSON.stringify(rolesInfo, null, 2)}
- Существующие задачи: ${JSON.stringify(existingTasksInfo, null, 2)}
- Доступные метки: ${JSON.stringify(labelsInfo, null, 2)}

СООБЩЕНИЕ: "${text}"

ПРАВИЛА:
1. **УМНОЕ ОБНОВЛЕНИЕ**: Если пользователь уточняет, дополняет или изменяет существующую задачу - ОБНОВЛЯЙ её, а не создавай новую
2. **НОВЫЕ ЗАДАЧИ**: Создавай только для действительно новых идей/планов
3. **РАЗБИВКА**: Сложные задачи разбивай на подзадачи
4. **СТРУКТУРА ЗАДАЧИ**:
   - **ЗАГОЛОВОК**: Краткое описание действия (2-5 слов). Что нужно сделать? Например: "Создать API", "Исправить баг", "Обновить дизайн"
   - **ОПИСАНИЕ**: Подробное описание сути задачи. Объясни ЧТО именно нужно сделать, ПОЧЕМУ это важно, КАК это должно работать, какие ДЕТАЛИ нужно учесть. Описание должно быть информативным и полным
5. **ДЕДЛАЙНЫ**: 
   - "До 30 ноября" = 30.11.2025 23:59 (включительно)
   - "До 30 ноября не включительно" = 29.11.2025 23:59 (исключительно)
   - Если время не указано = 00:00 (не отображается)
   - Анализируй контекст: включительно или исключительно
6. **ОБНОВЛЕНИЯ**: Обновляй только те поля, которые действительно изменились. Если заголовок тот же - НЕ указывай title
7. **МЕТКИ (LABELS)**:
   - Используй существующие метки из списка "Доступные метки", если они подходят по смыслу задачи
   - Если подходящей метки нет, но задача явно относится к определенной категории (например, "frontend", "backend", "дизайн", "тестирование"), создай новую метку в поле "newLabels"
   - При создании новой метки используй короткое название (1-2 слова) и опционально цвет в формате HEX (#RRGGBB)
   - Если задача не требует категоризации, не указывай labelId

ПРИМЕРЫ РАБОТЫ С МЕТКАМИ:
- "Нужно исправить баг на фронтенде" → используй существующую метку "frontend" если есть, или создай новую: { name: "frontend", color: "#3B82F6" }
- "Создать API для бэкенда" → используй существующую метку "backend" если есть, или создай новую: { name: "backend", color: "#10B981" }
- "Обновить дизайн интерфейса" → используй существующую метку "дизайн" если есть, или создай новую: { name: "дизайн", color: "#F59E0B" }
- "Протестировать функционал" → используй существующую метку "тестирование" если есть, или создай новую: { name: "тестирование", color: "#8B5CF6" }

ПРИМЕРЫ УМНОГО ОБНОВЛЕНИЯ:
- "Кстати, API нужно сделать REST" → обновить title: "Создать REST API"
- "Добавь к задаче про дизайн - нужно учесть мобильную версию" → обновить description с подробностями про мобильную версию
- "Перенеси дедлайн на завтра" → обновить deadline
- "Назначь это на Ивана" → обновить assignedUserId
- "Купить машину до 30 ноября" → обновить только deadline: "2024-11-30T23:59:59+03:00" (title не меняется!)
- "Купить машину до 30 ноября не включительно" → обновить только deadline: "2024-11-29T23:59:59+03:00" (title не меняется!)

ПРИМЕРЫ НОВЫХ ЗАДАЧ:
- "Еще нужно протестировать" → title: "Протестировать", description: "Провести полное тестирование функционала, проверить все сценарии использования, убедиться в отсутствии багов"
- "А что с документацией?" → title: "Обновить документацию", description: "Актуализировать документацию проекта, добавить описание новых функций, обновить примеры использования"
- "Хорошо бы добавить логи" → title: "Добавить логирование", description: "Реализовать систему логирования для отслеживания работы приложения, добавить логи ошибок, важных событий и действий пользователей"

ПРИМЕРЫ ДЕДЛАЙНОВ:
- "1 ноября" → "2024-11-01T00:00:00+03:00" (начало дня, время не отображается)
- "Завтра в 15:00" → "2024-01-16T15:00:00+03:00" (конкретное время)
- "До 30 ноября" → "2024-11-30T23:59:59+03:00" (включительно)
- "До 30 ноября не включительно" → "2024-11-29T23:59:59+03:00" (исключительно)
- "До конца года" → "2024-12-31T23:59:59+03:00" (включительно)

ФОРМАТ ОТВЕТА (только JSON):
{
  "newTasks": [
    {
      "title": "Краткое описание действия (что сделать)",
      "description": "Подробное описание сути задачи: что именно нужно сделать, почему это важно, как это должно работать, какие детали учесть",
      "assignedUserId": number | null,
      "assignedRoleId": number | null,
      "deadline": "YYYY-MM-DDTHH:mm:ss+03:00" | null,
      "labelId": number | null, // ID существующей метки, если подходит
      "labelName": string | null // Название новой метки для создания, если подходящей метки нет
    }
  ],
  "updatedTasks": [
    {
      "id": number, // ТОЛЬКО ID из существующих задач
      "title": "новое название" | null, // ТОЛЬКО если название действительно изменилось
      "description": "новое описание" | null, // ТОЛЬКО если описание изменилось
      "assignedUserId": number | null, // ТОЛЬКО если назначение изменилось
      "assignedRoleId": number | null, // ТОЛЬКО если роль изменилась
      "deadline": "YYYY-MM-DDTHH:mm:ss+03:00" | null, // ТОЛЬКО если дедлайн изменился
      "labelId": number | null // ТОЛЬКО если метка изменилась
    }
  ],
  "newLabels": [
    {
      "name": "название метки", // Короткое название (1-2 слова)
      "color": "#RRGGBB" | null // Опциональный цвет в формате HEX
    }
  ]
}`;
  },

  // Промпт для извлечения личных задач из текста/аудио
  extractPersonalTasks: (text: string, currentTime: Date, user: User, existingTasks: TaskEntity[] = []): string => {
    const existingTasksInfo = existingTasks.length > 0
      ? existingTasks.map(task => ({
          id: task.id,
          title: task.title,
          description: task.description || null,
          assignedUserId: task.assignedUserId || null,
          assignedRoleId: task.assignedRoleId || null,
          deadline: task.deadline ? (task.deadline instanceof Date ? task.deadline.toISOString().split('T')[0] : task.deadline) : null
        }))
      : [];

    return `Извлечение личных задач из сообщения пользователя.

КОНТЕКСТ:
- Время: ${currentTime.toISOString()}
- Пользователь: ${user.firstName} ${user.lastName || ''} (@${user.username})
- Существующие задачи: ${JSON.stringify(existingTasksInfo, null, 2)}

СООБЩЕНИЕ: "${text}"

ПРАВИЛА:
1. **УМНОЕ ОБНОВЛЕНИЕ**: Если пользователь уточняет, дополняет или изменяет существующую задачу - ОБНОВЛЯЙ её, а не создавай новую
2. **НОВЫЕ ЗАДАЧИ**: Создавай только для действительно новых планов/идей
3. **РАЗБИВКА**: Сложные задачи разбивай на подзадачи
4. **СТРУКТУРА ЗАДАЧИ**:
   - **ЗАГОЛОВОК**: Краткое описание действия (2-5 слов). Что нужно сделать? Например: "Купить продукты", "Изучить Python", "Записаться к врачу"
   - **ОПИСАНИЕ**: Подробное описание сути задачи. Объясни ЧТО именно нужно сделать, ПОЧЕМУ это важно, КАК это должно быть выполнено, какие ДЕТАЛИ нужно учесть. Описание должно быть информативным и полным
5. **ДЕДЛАЙНЫ**: 
   - "До 30 ноября" = 30.11.2025 23:59 (включительно)
   - "До 30 ноября не включительно" = 29.11.2025 23:59 (исключительно)
   - Если время не указано = 00:00 (не отображается)
   - Анализируй контекст: включительно или исключительно
6. **ОБНОВЛЕНИЯ**: Обновляй только те поля, которые действительно изменились. Если заголовок тот же - НЕ указывай title

ПРИМЕРЫ УМНОГО ОБНОВЛЕНИЯ:
- "Кстати, к врачу нужно завтра в 10 утра" → обновить deadline: "2024-01-16T10:00:00+03:00"
- "Добавь к изучению программирования - еще нужно практиковаться" → обновить description
- "Перенеси тренировку на вечер" → обновить deadline
- "Хочу изучить Python вместо JavaScript" → обновить title
- "Купить машину до 30 ноября" → обновить только deadline: "2024-11-30T23:59:59+03:00" (title не меняется!)
- "Купить машину до 30 ноября не включительно" → обновить только deadline: "2024-11-29T23:59:59+03:00" (title не меняется!)

ПРИМЕРЫ НОВЫХ ЗАДАЧ:
- "Еще нужно купить продукты" → title: "Купить продукты", description: "Составить список необходимых продуктов, проверить наличие дома, купить все необходимое в магазине"
- "А что с английским?" → title: "Изучить английский", description: "Продолжить изучение английского языка, определить текущий уровень, составить план занятий, выбрать материалы для изучения"
- "Хорошо бы начать читать" → title: "Начать читать", description: "Выбрать книгу для чтения, определить время для чтения, составить план чтения, начать регулярное чтение"

ПРИМЕРЫ ПРАВИЛЬНЫХ ДЕДЛАЙНОВ:
- "До завтра" → "2024-01-15T23:59:59+03:00" (сегодня 23:59)
- "До конца недели" → "2024-01-21T23:59:59+03:00" (воскресенье 23:59)
- "До конца месяца" → "2024-01-31T23:59:59+03:00" (конец месяца)
- "До 2026 года" → "2025-12-31T23:59:59+03:00" (31 декабря 2025)
- "1 ноября" → "2024-11-01T00:00:00+03:00" (начало дня, время не отображается)
- "Завтра в 10:00" → "2024-01-16T10:00:00+03:00" (конкретное время)

ФОРМАТ ОТВЕТА (только JSON):
{
  "newTasks": [
    {
      "title": "Краткое описание действия (что сделать)",
      "description": "Подробное описание сути задачи: что именно нужно сделать, почему это важно, как это должно быть выполнено, какие детали учесть",
      "assignedUserId": null,
      "assignedRoleId": null,
      "deadline": "YYYY-MM-DDTHH:mm:ss+03:00" | null
    }
  ],
  "updatedTasks": [
    {
      "id": number, // ТОЛЬКО ID из существующих задач
      "title": "новое название" | null,
      "description": "новое описание" | null,
      "assignedUserId": null,
      "assignedRoleId": null,
      "deadline": "YYYY-MM-DDTHH:mm:ss+03:00" | null
    }
  ]
}`;
  },

};